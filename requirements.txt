numpy>=1.26
pandas>=2.1
pyarrow>=15.0
PyYAML>=6.0
scikit-learn>=1.4
scipy>=1.11
matplotlib>=3.8
seaborn>=0.13
yfinance>=0.2.40
joblib>=1.3
requests>=2.31
statsmodels>=0.14
psutil>=5.9
exchange-calendars>=4.5
pytest>=8.0
python-docx>=1.1

# PyTorch defaults to a CPU wheel on PyPI. For the RTX 5090 environment use:
# py -m pip install --upgrade torch --index-url https://download.pytorch.org/whl/cu130
torch>=2.9

# Recommended Phase 1 optional baselines and robustness providers. These are
# also available through pyproject extras, e.g.:
#   py -m pip install -e ".[boosting,econometrics,providers]"
xgboost>=2.0
lightgbm>=4.0
arch>=7.0
pandas-datareader>=0.10
ccxt>=4.0

catboost>=1.2
python-dotenv>=1.0
